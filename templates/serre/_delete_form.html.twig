<form method="post" action="{{ path('app_serre_delete', {'id': serre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ serre.id) }}">
    <button class="btn">Delete</button>
</form>
