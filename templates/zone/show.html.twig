{% extends 'base.html.twig' %}

{% block title %}Zone{% endblock %}

{% block body %}
    <h1>Zone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zone.id }}</td>
            </tr>
            <tr>
                <th>NomZone</th>
                <td>{{ zone.nomZone }}</td>
            </tr>
            <tr>
                <th>TypeZone</th>
                <td>{{ zone.typeZone }}</td>
            </tr>
            <tr>
                <th>Superficie</th>
                <td>{{ zone.superficie }}</td>
            </tr>
            <tr>
                <th>EtatZone</th>
                <td>{{ zone.etatZone }}</td>
            </tr>
            <tr>
                <th>CultureAssociee</th>
                <td>{{ zone.cultureAssociee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zone_index') }}">back to list</a>

    <a href="{{ path('app_zone_edit', {'id': zone.id}) }}">edit</a>

    {{ include('zone/_delete_form.html.twig') }}
{% endblock %}
